/* ==========================================================================
   App configuration – single place to adjust endpoints and defaults.
   Change API_ENDPOINT when your backend is deployed.
   ========================================================================== */

const API_ENDPOINT = "http://localhost:3000/api/submit";

/* Max length of the description field (must match the HTML maxlength). */
const DESCRIPTION_MAX = 2000;
